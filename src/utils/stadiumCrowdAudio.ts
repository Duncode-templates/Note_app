// Dynamic Stadium Ambient & Crowd Chant Engine - Disabled/Silenced as requested
export function startMatchCrowd(_options: { isPractice?: boolean; isSurvival?: boolean } = {}) {
  // Crowd sound removed as requested
}

export function stopMatchCrowd() {
  // Crowd sound removed as requested
}

export function setCrowdExcitement(
  _level: 'normal' | 'buildup' | 'shot_inflight' | 'goal' | 'save_reaction' | 'miss_groan'
) {
  // Crowd sound removed as requested
}

export function updateCrowdMuteState(_isMuted: boolean) {
  // Crowd sound removed as requested
}

